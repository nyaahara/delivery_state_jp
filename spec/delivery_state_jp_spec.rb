require 'spec_helper'

describe DeliveryStateJp do
  it 'has a version number' do
    expect(DeliveryStateJp::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
